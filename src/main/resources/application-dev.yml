spring:
  datasource:
    url: ${URL_DATABASE}
    username: ${USER}
    password: ${PASSWORD}
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
        dialect: ${URL_DATABASE_DIALECT}
        format_sql: true
    show-sql: true
